from random import randint
numeros = [0,0,0,0,0]

def sorteio(lista):
    cont == 0
    while cont != 5:
        aleatorio = randint(1, 10)
        numeros[cont].append(aleatorio)
        cont += 1
    print(numeros)

print(sorteio(numeros))